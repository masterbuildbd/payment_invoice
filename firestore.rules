rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for all documents to support local/custom authentication mechanism
    // and offline synchronization fallbacks used in the application.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
